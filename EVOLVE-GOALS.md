# 演進目標（Evolve Goals）— spectyn-mesh

本檔案是驅動 `spectyn autoevolve --goals` 的精選里程碑佇列（milestone queue，里程碑待辦清單）。可自由編輯：任何不符合 `- [ ]` / `- [x]` 格式的行，在來回讀寫（round-trip）時都會被保留，所以筆記與章節標題都會原樣留存。

**挑選的目標要小到能在一次 commit（提交）內交付。**「新增帶一個測試的 HMAC 輪替端點（endpoint）」是合適的大小；「重建整個叢集管理器（cluster manager）」則不是。大型目標應放進 MASTER-PLAN.md，再切成此處的條目。

## 待辦（Pending）

- [ ] MULTI-DEV Gap 1 — GitHub Actions 發行矩陣（release matrix）：位於 `.github/workflows/release-matrix.yml` 的工作流程（workflow），由 `v*` 標籤推送觸發；為 {macos-arm64, macos-x86_64, windows-x86_64, linux-x86_64, linux-arm64} 各平台建置（build）並簽署（sign）spectyn 二進位檔；以 `spectyn-<platform>.tar.gz` 上傳到 release。跨平台快取 cargo target 使整體執行時間 <5min。驗收（Acceptance）：打一個 `v0.1.1-test` 標籤，觀察全部 5 個產物（artifact）出現在 releases 頁面。
- [ ] MULTI-DEV Gap 3 — `spectyn doctor --mesh`：載入 agents.base.toml 的子命令（subcommand），逐一遍歷已設定的對等節點（peer），各自 GET /rpc/ping，解析 wire_version + core_sha + node_name，印出對齊的表格，每個 peer 標示 ✓/⚠/✗/○ 狀態。退出碼 0 全綠 / 1 降級（degraded） / 2 損壞（broken）（依文件的 Rule 6）。`--fix` 旗標嘗試自動復原（提示輪替 secret、建議 `spectyn upgrade`）。
- [ ] MULTI-DEV Gap 4 — `spectyn upgrade`：子命令從 `gh release download`（或 curl + 簽章驗證）拉取符合此二進位檔平台的最新發行標籤，驗證維護者（maintainer）簽章，原子式替換（atomic-swap）二進位檔（bootout LaunchAgent → swap → ad-hoc codesign → bootstrap → healthcheck）。healthcheck 失敗則回滾（roll back）。印出升級前/後版本。codesign 步驟見 `docs/CO-EVOLUTION.md` Tier 3 + commit `85c8377`。
- [ ] MULTI-DEV Gap 5 — `agents.toml` 拆成 base + local 兩份：`agents.base.toml` 簽入（check in）儲存庫（provider 定義、agent.* 角色、cluster.peers 清單）；`~/.spectyn-mesh/local.toml` 為每台機器特有（node_name、cluster_secret、api_key 覆寫值），並 `.gitignored`。`AgentsConfig::load()` 先讀 base，再把 local 深度合併（deep-merge）疊上去。`spectyn keys generate-cluster-secret` 寫入 16 個隨機位元組到 local.toml。既有 `agents.toml` 的遷移路徑：首次載入時拆分，將原本統一的檔案備份為 `agents.toml.bak`。
- [ ] MULTI-DEV Gap 6 — `.spectyn-core-lock.json` 鎖檔（lock file）慣例：當某個 session（工作階段）打算編輯 `core/src/mesh.rs` / `serve.rs::rpc_*` / `keys.rs` 時，它在第一次 commit 寫入該鎖檔（owner_session、intent、expires_at_ms = now+1h），並在最後一次 commit 刪除它。若鎖被持有超過 2h，`spectyn doctor --mesh` 會發出警告。`.git/hooks/pre-commit.d/` 中的 pre-commit 掛鉤（hook），若 `.spectyn-core-lock.json` 由他人持有，會拒絕對那些路徑的 commit。

- [ ] CO-EVO Phase 1 (Sandbox，沙盒)：讓 autoevolve 預設拒絕寫入 `core/`、`app/`、`templates/`、`scripts/` 底下的任何東西。僅允許清單（allowlist）放行 `~/.spectyn-mesh/extensions/{prompts,skills,hooks}/` 以及目標佇列檔本身。新增一個 `--allow-core-evolve` 旗標，可對單次呼叫解除該限制。驗收：整合測試以一個要求編輯 `core/src/lib.rs` 的目標執行 autoevolve，斷言該檔案維持不變且出現清楚的錯誤訊息。見 `docs/CO-EVOLUTION.md` Tier 1。
- [ ] CO-EVO Phase 2 (Recipe export/import，配方匯出/匯入)：`spectyn evolve publish <session-id>` 在 `~/.spectyn-mesh/recipes/<sha>.json` 產出一份自我完備（self-contained）的已簽署配方（recipe），內含 EvolveCheckpoint、`git format-patch` 二進位區塊（blob，若有）、平台元組（tuple）以及來源節點的 ed25519 簽章。`spectyn evolve adopt <recipe-path>` 驗證簽章、在臨時工作樹（scratch worktree）套用修補檔（patch）、執行 `cargo test`，並將變更暫存（stage）以供人工審查。兩個單元測試：publish/adopt 來回往返；簽章不符遭拒。見 `docs/CO-EVOLUTION.md` Tier 2。
- [ ] CO-EVO Phase 3 (Trust chain，信任鏈)：`spectyn keys init` 在 `~/.spectyn-mesh/keys/{ed25519.priv,ed25519.pub}`（0600 權限）產生每位使用者專屬的 ed25519 金鑰對。每次 `evolve publish` 都簽署配方主體（body）。儲存庫根目錄的 `MAINTAINERS.md` 列出受信任的公鑰；`spectyn evolve adopt` 會將簽章與此清單交叉比對，除非加 `--trust-anyway`，否則拒絕未簽署/不受信任的配方。測試：竄改主體位元組，斷言驗證失敗。
- [ ] CO-EVO Phase 4 (Core-PR pipeline，核心 PR 管線)：當某配方的修補檔觸及 `core/` 或 `app/` 底下的路徑時，`spectyn evolve publish` 不只在本機儲存——它會透過 `gh api` 分叉（fork）上游儲存庫、把修補檔推送到新分支、以 EvolveCheckpoint 的 markdown 作為 PR 內文開一個 PR，並以 `auto-evolve` + 來源平台為其加標籤。需要已認證的 `gh` CLI。測試：乾跑（dry-run）模式，印出將會產生的 PR 內文卻不實際呼叫 gh。見 `docs/CO-EVOLUTION.md` Tier 3。
- [ ] CO-EVO Phase 5 (CI gate + automerge bot，CI 關卡 + 自動合併機器人)：位於 `.github/workflows/co-evolution.yml` 的 GitHub Actions 工作流程，對每個帶 `auto-evolve` 標籤的 PR 執行跨平台測試（mac/win/linux）、CodeQL、cargo-audit、clippy `-D warnings`。敏感路徑（`core/src/auth/`、`core/src/mesh.rs`、`core/src/keys.rs`、`core/src/serve.rs`、`templates/*.tmpl`）無論 CI 是否全綠都需要人工審查標籤。自動合併機器人在下列情況落地（land）該 PR：(a) 3 個平台全綠 且 (b) 未觸及敏感路徑 且 (c) 由 `MAINTAINERS.md` 中的金鑰簽署。否則：留給人工審查。
- [ ] CO-EVO Phase 6 (Sync，同步)：`spectyn evolve sync` 從上游拉取最新的已標籤發行版，並以前一版二進位檔內嵌的維護者金鑰驗證簽章；`spectyn upgrade` 原子式完成二進位檔替換（download → verify → ad-hoc-codesign → bootout LaunchAgent → swap → bootstrap）。每日的 `launchd` 計時器可選，透過 `spectyn evolve sync schedule install` 安裝。測試：模擬簽章驗證失敗時的回滾——舊二進位檔被保留。

## 已完成（Done）
- [x] (2026-05-01) MULTI-DEV Gap 2 — RPC 上的線路版本（Wire version）：`core/src/lib.rs` 中的 `WIRE_VERSION = 1`；`core/src/serve.rs` 中的 `with_wire_version()` 輔助函式在每個面向 peer 的 RPC（`/rpc/ping`、`/rpc/peers`、`/rpc/message`、`/rpc/task/{assign,status}`、`/rpc/evolve-handoff`）注入該欄位；`check_wire_version()` 對 `wire_version` 超過本機的傳入主體以 HTTP 400 拒絕，並給出清楚的「run `spectyn upgrade`」提示。`/rpc/ping` 加入 `spectyn_version` + `core_sha`（SPECTYN_GIT_HASH）。`serve::wire_version_tests` 中 4 個單元測試（lib 共 288 個，0 失敗）。
- [x] (2026-05-01) MAC fix(release) — 對每個 macOS 二進位檔放置點做 Ad-hoc codesign：scripts/install-mac.sh + .github/workflows/release-desktop.yml + 新增 scripts/build-mac.sh。補上 85c8377 只為 `spectyn service install` 修掉的安裝路徑 SIGKILL 缺口。shellcheck + actionlint 乾淨；smoke-mac.sh 29/0/1。
- [x] (2026-04-30 sha=0031a79) BUG FIX（已交付，見 commit log）：長回覆在 4096 tokens 處被截斷——將預設值提升到 8192 + 加上 SPECTYN_MAX_TOKENS 環境變數覆寫。在此追蹤只是為了讓 EVOLVE-GOALS.md 的稽核軌跡（audit trail）反映使用者回報的 bug，而非僅有前瞻性的功能。
- [x] (2026-04-30 sha=dafb3e5) TUI：文字輸出區絕對不可與輸入框重疊。目前當輸入框長成多行時，串流輸出的底部幾行會被輸入區遮住。修正版面，使回捲（scrollback）的 Paragraph 尺寸為 `total_height - input_height`（其中 input_height 每一幀都依換行後的輸入寬度重新計算），且自動捲動讓最新一行剛好顯示在輸入框上方。驗收：TestBackend 測試渲染一段長串流回應、把輸入框長到 5 行，並斷言輸出的最後一行仍可見（未被輸入框遮蓋）。
- [x] (2026-04-30 sha=dafb3e5) TUI：當 spectyn 忙碌時（LLM 串流中、工具執行中、輸入停用），在「spectyn v0.1.0」標題的緊鄰左側立即顯示一個轉圈動畫（spinner）——忙碌時動畫播放，輸出結束且輸入重新啟用時停止並清除。要求：spinner 字形約以 10 fps 旋轉；乾淨停止不留殘影幀；標題文字不閃爍；從輸入被鎖住的那一刻起即可見。驗收：ratatui TestBackend 測試，斷言忙碌狀態下 spinner 儲存格非空，閒置狀態下等於原始字元。
- [x] (2026-04-30 sha=358e282) 為 `spectyn evolve goals next` 新增一個 `--file` 旗標，當 stdout 被導管（pipe）時，只在 stdout 輸出目標文字（無 eprintln 裝飾）——已半數成立。以 tempfile 上的單元測試驗證。
- [x] (2026-04-30 sha=358e282) 讓 `spectyn evolve goals list` 接受一個 `--json` 旗標，將 `{"pending": [...], "done": [...]}` 印到 stdout 供腳本使用。不要破壞既有的人類可讀輸出。對一份範例檔做一個測試。
- [x] (2026-04-30 sha=358e282) 新增一個 `spectyn evolve goals add "<text>"` 子命令，將一個新的待辦目標附加到 `## Pending` 之下並儲存檔案。包含一個涵蓋來回往返的單元測試（load → add → save → load → next_pending 符合該新條目）。
