[package]
name = "pm-types"
version = "0.4.0"
edition = "2021"
description = "Shared type definitions for Phantom Mesh"
license = "MIT OR Apache-2.0"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
uuid = { version = "1", features = ["v4", "serde"] }
thiserror = "1"
fnv = "1"

[dev-dependencies]
dirs = "5"
