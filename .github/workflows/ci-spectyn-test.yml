name: CI spectyn-test (scenario harness)

# Black-box scenario harness from scripts/spectyn-test/. Complements the
# in-tree `cargo test --lib` (which ci-fast.yml already runs) by exercising
# the wire protocol, on-disk state, and CLI surface of a built spectyn
# binary against a live `spectyn serve`.
#
# CI-safe scenario subset (no real LLM keys, no Tailscale, no remote peer):
#   01 doctor baseline
#   03 RPC HMAC — bad secret rejected
#   09 events.jsonl grows on activity
#   10 RPC bogus job-id error
#   12 mock LLM end-to-end (uses lib/mock-llm-server.py — $0, sub-50ms)
#   18 MCP stdio tools/list
#   22 service status reports autostart registration
#   23 RPC malformed body fails cleanly (no 5xx / no panic)
#   24 RPC missing auth header rejected
#
# Other scenarios (02/04/06/11/13–17/19–21/25) need real provider keys or
# a multi-host cluster — they'll be added back once we have repo secrets
# wired up for tier-rate-limited free providers (groq / opencode-zen).

on:
  push:
    branches: [main]
    paths:
      - 'core/**'
      - 'scripts/spectyn-test/**'
      - '.github/workflows/ci-spectyn-test.yml'
  pull_request:
    branches: [main]
    paths:
      - 'core/**'
      - 'scripts/spectyn-test/**'
      - '.github/workflows/ci-spectyn-test.yml'

# Cancel superseded runs on the same branch/PR — saves CI minutes.
concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

env:
  CARGO_TERM_COLOR: always
  RUST_BACKTRACE: 1

jobs:
  scenario-harness:
    name: spectyn-test scenario harness (CI-safe subset)
    runs-on: ubuntu-latest
    timeout-minutes: 20

    steps:
      - uses: actions/checkout@v4

      - name: Install Rust stable
        uses: dtolnay/rust-toolchain@stable

      - name: Cache cargo registry and build
        uses: Swatinem/rust-cache@v2
        with:
          workspaces: core -> target

      - name: Install runtime deps
        run: |
          sudo apt-get update
          sudo apt-get install -y libssl-dev openssl curl jq
          # Python is preinstalled on ubuntu-latest; just confirm.
          python3 --version

      - name: Build spectyn (release)
        working-directory: core
        run: cargo build --release --bin spectyn

      - name: Install spectyn on PATH
        run: |
          install -m 755 core/target/release/spectyn /usr/local/bin/spectyn
          spectyn --version

      - name: Bootstrap minimal config (no real LLM keys)
        run: |
          mkdir -p ~/.spectyn-mesh
          cat > ~/.spectyn-mesh/agents.toml <<'EOF'
          [core]
          host = "127.0.0.1"
          port = 7879

          [cluster]
          # Match SPECTYN_CLUSTER_SECRET default in scripts/spectyn-test/lib/common.sh
          secret = "ci-test-cluster-secret"

          # No [providers.*] sections — scenarios that need a real LLM will
          # skip cleanly via exit 77 (graceful no-key handling) or fail with
          # a clean error (precisely what scenario 07 verifies). Scenario 12
          # provides its OWN mock provider per-test in a temp cwd.
          EOF

      - name: Start spectyn serve (background)
        run: |
          # Detached so the workflow step can finish; harness scenarios
          # talk to it over HTTP. Logs land in /tmp for upload on failure.
          nohup spectyn serve > /tmp/spectyn-serve.log 2>&1 &
          echo $! > /tmp/spectyn-serve.pid
          echo "spectyn serve PID: $(cat /tmp/spectyn-serve.pid)"

      - name: Wait for /healthz
        run: |
          for i in $(seq 1 30); do
            if curl -sf http://127.0.0.1:7879/healthz > /dev/null; then
              echo "✓ spectyn serve up after ${i}s"
              exit 0
            fi
            sleep 1
          done
          echo "✗ spectyn serve never responded on /healthz"
          tail -50 /tmp/spectyn-serve.log
          exit 1

      - name: Run CI-safe scenario subset
        env:
          SPECTYN_BIN: /usr/local/bin/spectyn
          SPECTYN_HOST: 127.0.0.1
          SPECTYN_PORT: '7879'
          SPECTYN_CLUSTER_SECRET: ci-test-cluster-secret
          NO_COLOR: '1'
        run: |
          # Curated prefix list — see workflow header for what each tests.
          bash scripts/spectyn-test/harness.sh 01 03 09 10 12 18 22 23 24

      - name: Stop spectyn serve
        if: always()
        run: |
          if [ -f /tmp/spectyn-serve.pid ]; then
            kill "$(cat /tmp/spectyn-serve.pid)" 2>/dev/null || true
          fi

      - name: Upload spectyn-serve log on failure
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: spectyn-serve-log
          path: |
            /tmp/spectyn-serve.log
            ~/.spectyn-mesh/events.jsonl
            ~/.spectyn-mesh/crashes/
          if-no-files-found: ignore
          retention-days: 7
