DEFERRED / HUMAN-LED — DO NOT run autonomously. Adds local offline embeddings via `ort` (ONNX Runtime) + all-MiniLM-L6-v2 quantized model. This adds a HEAVY dependency to core/Cargo.toml + a model-fetch step + has cross-platform build implications (Win/Mac/Linux/mobile). It must be designed + reviewed by a human, not implemented by an autonomous worker. This file documents the task; it is intentionally not dispatchable. Scope when picked up: add ort behind a feature flag, a model fetch/verify step (SPEC-25 §17.3), local embed() impl, and prove offline recall — with explicit per-platform build verification.
