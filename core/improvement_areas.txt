 phantom-mesh 代碼改進優先區域（基於 clippy 警告）：

 1. src/context.rs 模組
    - 警告類型：可合併的if語句（4處）、手動字符串前綴 stripping（2處）、低效的雙端迭代器 last 調用（2處）
    - 建議：重構條件邏輯以合併嵌套if，使用標準庫方法如 strip_prefix、next_back 替換手動實現

 2. 函數參數過多問題
   - 位置：src/agent.rs:169 (run_inner 函數)、src/bin/phantom.rs:1059 (repl 函數)
   - 警告：函數參數數量超過閾值（8/7）
   - 建議：將相關參數封裝為結構體以改善 API 設計和可讀性

 3. 重複條件語句和手動實現
   - 位置：多處發現 identical if blocks（if_same_then_else）、手動字符串操作（manual_split_once、manual_find）、手動數學運算（manual_is_multiple_of）
   - 建議：使用標準庫替換手動實現，簡化條件邏輯以消除重複分支